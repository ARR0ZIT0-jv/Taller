services:
  - type: web
    name: uninetwork-api
    env: node
    rootDir: uninetwork/backend
    buildCommand: npm install
    startCommand: npm run seed && npm start
    envVars:
      - key: NODE_VERSION
        value: 18.20.2
      - key: JWT_SECRET
        generateValue: true
